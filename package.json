{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npm install && lerna clean --yes && lerna bootstrap",
    "test": "lerna run --scope playwright-module-loader build && lerna run test"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
