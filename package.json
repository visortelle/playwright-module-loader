{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna clean --yes && lerna bootstrap",
    "test": "npm run build && cd packages/playwright-module-loader && npm test"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
